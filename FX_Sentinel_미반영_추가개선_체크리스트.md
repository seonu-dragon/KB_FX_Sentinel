# FX Sentinel 추가개선 체크리스트 — 종결 기록

최초 작성: 2026-07-12 · **종결 감사: 2026-07-17**
대상 파일: `FX_Sentinel_demo_ui.html`

> **결론: 12개 항목 전부 반영 완료. 이 문서는 더 이상 "남은 일 목록"이 아니다.**
>
> 2026-07-12에 P0 5건 / P1 6건 / P2 1건을 남은 문제로 적었으나, 이후 작업으로 전부 반영됐다.
> 문서만 갱신되지 않은 채 남아 "아직 안 한 일"로 오해될 소지가 있어, 2026-07-17에 데모 HTML을
> 항목별로 대조 감사하고 **반영 위치를 적은 종결 기록으로 대체**한다.
>
> 감사 시점 상태: UI 스모크 테스트 **289개 통과 / 0 실패**, 문서 정합성 검사 **16개 통과 / 0 실패**.

---

## 반영 결과 요약

| # | 원래 지적 | 우선순위 | 상태 | 반영 위치 (감사 근거) |
|---|---|---|---|---|
| 1 | 고객/RM/심사 화면 정보량 미분리 | P0 | ✅ | `data-mode` 모드 전환 + RM passcode 게이트 + `④ 심사 자료` 탭 분리 |
| 2 | 첫 화면 설명 문구가 길고 어려움 | P0 | ✅ | 고객 모드 전용 문구 전환, 법적 고지는 `.guard` 박스로 분리 |
| 3 | 전문용어 조기 노출 | P0 | ✅ | `📖 쉬운 용어 설명` 섹션 + `internal-only` 클래스로 RM 전용 게이팅 |
| 4 | 입력 폼이 복잡함 | P0 | ✅ | 기본 5항목 + `<details class="advform">고급 설정` 접기 |
| 5 | 한 페이지에 기능 과다 | P0 | ✅ | 심화 4탭(① 결과·근거 / ② 상담·진행 / ③ 준법·데이터 / ④ 심사 자료) |
| 6 | 고객 CTA 분산 | P1 | ✅ | 고객 모드에서 `hd-cta`·`book` 모두 `KB 상담 요청하기`로 전환 |
| 7 | 상품 적합성 판정 근거 부족 | P1 | ✅ | 카드 내 자격 블록 (형태만 다름 — 아래 참고) |
| 8 | KB 내부 연동 시퀀스·RM payload | P1 | ✅ | `⇄ KB 내부 연동 시퀀스` · `{} RM 업무함 전달 payload` 카드 |
| 9 | 파일럿 KPI 측정 설계 부족 | P1 | ✅ | `✓ 파일럿 KPI 측정 설계` 카드 |
| 10 | 권한/개인정보/감사로그 운영표 | P1 | ✅ | `§ 권한 매트릭스(RBAC)` — 고객/RM/지점장/준법·AML/관리자 |
| 11 | 접근성이 포커스 링만으로 부족 | P1 | ✅ | reduced-motion · coarse pointer 44px · `aria-describedby` · 명도대비 (스모크 테스트가 검증) |
| 12 | 제재/거래국 "정상" 표현 위험 | P2 | ✅ | 제재 조회 메타(무엇·언제·리스트 버전) + `데모 데이터 · 실시간 조회 아님` 배지 |

---

## 4번 — 라벨 치환 결과

권장했던 문구가 그대로 반영됐다.

```text
익스포저 확실성       → 거래가 확정됐나요?
예산환율 K            → 회사 기준환율 (원)
옵션 프리미엄 현금여유 → 옵션 비용 낼 여유
품목 / HS 코드        → 품목 코드 (모르면 비워도 됨)
```

기본 폼: 기업명 · 수입/수출 · 결제 금액 · 결제 만기 · 거래국
고급 설정(접기): 기업 유형 · 회사 기준환율 · 결제 통화 · 결제 방식 · 품목 코드 · 거래 확정 여부 · 여신 한도 · 옵션 비용 여유 · 예상 마진율

---

## 7번 — 표가 아니라 블록으로 반영한 이유

원래 제안은 `항목 / 현재 입력값 / 판정` 3열 표였다. 실제 구현은 표 대신 상품 카드 안의 자격 블록인데,
**담긴 정보는 제안보다 많다.**

카드가 내는 것:

```text
자격요건 확인 게이지 (충족 n / 전체)
충족      — 폼 입력에서 파생된 사실 목록
미확인    — RM 이 원장·심사로 채울 목록 (rmChecks 와 동일 소스)
차단      — 사유 + 상세 + 해제조건
서류 준비 n/m — 확인 / 필요
신청채널 · RM 확인사항 · 다음 액션 · KB 경로
```

표로 바꾸지 않기로 한 근거:

- 값이 하드코딩이 아니라 **폼 입력에서 파생**된다. 코드 주석에 이유가 남아 있다 —
  *"하드코딩하면 입력이 바뀔 때 카드가 거짓말을 한다"*.
- `충족`/`미확인`이 `ELIG` 단일 소스와 `rmChecks`로 묶여 있어, 표로 옮기면 같은 사실을 두 곳에
  적게 되고 갈라질 위험이 생긴다 (문서 정합성 검사의 `ELIG 단일 소스` 항목이 이걸 지킨다).
- 즉 **표는 형태만 바뀌고 회귀 위험만 늘린다.** 지적의 본질("추천 이유보다 판정 근거")은 이미 충족.

---

## 감사에서 같이 고친 것

- `tests/ui_smoke_test.py` — 한글 콘솔(cp949)에서 **검증을 다 마친 뒤 요약을 출력하다가**
  `UnicodeEncodeError`(U+2212)로 죽어 통과/실패 수가 안 보이던 문제. `sys.stdout.reconfigure`로 수정.
  검증 로직에는 영향 없음(터지기 전까지 전부 PASS였음).

---

## 당시 판정 vs 현재

2026-07-12 판정:

```text
실제 고객 사용 편의성: 보통
실서비스 전환 설계: 아직 보강 필요
```

2026-07-17 기준으로 위 두 축이 이 체크리스트의 P0/P1 반영으로 해소됐다. 고객/RM/심사 3모드 분리와
용어·입력 경량화가 편의성을, 연동 시퀀스·RM payload·RBAC·KPI 측정설계가 전환 설계를 메웠다.

**남은 리스크는 이 문서 범위 밖이다 — 예선 제출물 중 기술설명서 PPT 미작성(예선 마감 8/3 16:00).**
