# (A) FX-EWI 산출식 상세 설계

> FX Sentinel의 자체 지표 **FX-EWI(FX Early-Warning Index)** 의 수학적 정의서.
> 상위 문서: `FX_Sentinel_청사진.md` §4. · 작성일: 2026-07-07 · v0.1
> 목표: "이름 붙일 수 있는 자체 지수 + 산출 방법론 + 검증"을 이 한 문서로 못 박는다.

---

> ## ⚠️ v2 정정 배너 (2026-07-08 실증 · 2026-07-17 최종) — `F_전략재정렬_v2.md`가 최상위
> 이 문서의 **§6.1 "FX-EWI가 GARCH를 유의하게 이긴다"는 목표는 실증으로 기각**됐다.
> walk-forward OOS AUC 0.577 < 레벨 베이스라인 0.596, 구제 캘리브레이션 3라벨 전부 패배 →
> **FX-EWI는 알파가 아니라 XAI(원인분해)로 강등**하고, 자체 지표 가치는 **BBP(예산환율 이탈확률)** 로 이동했다.
> 아래 설계는 **당시의 계획과 그 계획이 어떻게 반증됐는지**를 남기기 위한 기록이다(F 원칙: 낡은 주장을 몰래 지우지 않는다).
> 현재 배포 화면의 숫자 = `state/*.json` 실측 = `F` 문서 기준.

---


---

## 1. 설계 원칙

1. **예측 대상을 먼저 정의한다.** FX-EWI는 감(感)이 아니라 **"향후 N영업일 내 환리스크 고조" 확률**을 추정하도록 학습·보정한다. → 지표가 곧 검증 가능한 대상이 됨.
2. **룩어헤드 금지 (signal_lag=1).** 시점 t의 FX-EWI는 **t 종가까지의 정보만** 사용한다. 라벨(정답)만 미래(t+1…t+N)를 본다. (AUTO_BUY_SELL 원칙 계승)
3. **모든 구성요소를 0~1로 표준화**한 뒤 합성한다. 스케일이 다른 요인을 공정하게 결합.
4. **가중치는 손으로 튜닝하지 않는다.** 지도학습으로 라벨에 맞춰 보정(calibration)하고, **walk-forward + PBO/DSR로 과최적화를 검증**한다.
5. **해석가능성 유지.** 합성은 (정규화된) 로지스틱 선형결합 → 각 요인의 기여도를 %로 분해해 XAI에 그대로 쓴다.

---

## 2. 표기 (Notation)

| 기호 | 의미 |
|---|---|
| `P_t` | 원/달러(USD/KRW) 일 종가 |
| `r_t = ln(P_t / P_{t-1})` | 로그수익률 |
| `σ_t` | 시점 t의 조건부 변동성 추정치 |
| `u_t = r_t / σ̂_{t-1}` | 표준화 수익률 (직전 변동성으로 정규화) |
| `N` | 예측 지평(기본 **10영업일**, 5·10·20 비교 대상) |
| `z(·)` | 롤링 z-score (평균/표준편차는 과거 창에서만) |
| `Φ_L(x)=1/(1+e^{-x})` | 로지스틱 스쿼시 (0~1) |

핵심 요소 5종: **V**(변동성 국면), **J**(점프 리스크), **C**(클러스터링 강도·Hawkes), **M**(거시 이벤트 근접도), **S**(뉴스·심리 텐션).

---

## 3. 구성요소별 수식

### 3.1 V — 변동성 국면 (Volatility Regime)

**아이디어:** 단기 변동성이 중기 대비 팽창 중이면 위험 고조.

EWMA 분산(RiskMetrics, λ=0.94):
```
σ²_ewma,t = λ · σ²_ewma,t-1 + (1-λ) · r²_t
```
단기(5d)·중기(20d) 실현변동성:
```
RV_short = sqrt( mean(r²) over last 5d )
RV_med   = sqrt( mean(r²) over last 20d )
```
**변동성 추정량 — Yang-Zhang (2026-07-07 피드백 반영):** Parkinson은 장중 고저만 써서 원/달러의 **야간 개장 시가 갭**(FOMC 등 역외 NDF 충격)을 통째로 누락 → 변동성 과소평가. → **Yang-Zhang 추정량**으로 교체(시가갭 + 장중 + 종가 결합, 드리프트 독립·갭 편향 제거):
```
σ²_YZ = σ²_overnight + k·σ²_open-close + (1−k)·σ²_RS   # RS=Rogers-Satchell
# σ²_overnight=이전종가→당일시가 갭 분산, σ²_RS=장중 고저종시 기반, k=Yang-Zhang 가중
```
(OHLC 확보 필요. OHLC 불가 시 종가 기반 EWMA로 폴백.)

**국면비율 → 표준화:**
```
RVratio_t = RV_short / RV_med
V_t = Φ_L( z(RVratio_t) )        # 0~1, >0.5면 변동성 확장 국면
```

**기본 파라미터:** λ=0.94, 단기 5d, 중기 20d, z-score 창 252d(학습셋 내에서만 추정).

---

### 3.2 J — 점프 리스크 (Jump Concentration)

**아이디어:** 최근에 급변(꼬리 사건)이 잦으면 위험 고조.

점프 판정(표준화 수익률 임계 초과):
```
u_t = r_t / σ_ewma,t-1
Jump_t = 1{ |u_t| > k }          # k=3 기본 (학습셋 분위수로 조정 가능)
```
지수감쇠 가중 최근 점프 밀도:
```
J_raw = Σ_{i=1..m} e^{-i/τ_J} · Jump_{t-i+1}
J_max = Σ_{i=1..m} e^{-i/τ_J}                 # 이론적 최대치(모든 날 점프 시)
J_t   = J_raw / J_max                          # 0~1, 유계 스케일링
```
> **⚠️ z-score 금지(2026-07-07 피드백 반영):** 점프(k=3)는 희소 사건이라 `J_raw`가 대부분 0인 계열이다. 여기에 z-score를 씌우면 **평상시 Φ_L(z(0))≈0.47 → 점프 시 즉시 ≈1.0**으로 튀는 **이진 계단함수**로 전락한다(분모 std가 너무 작아 포화). → z-score 대신 **이론적 최대치 `J_max`로 나눠 유계화**하면 최근성·빈도가 연속적으로 반영된다. (더 정밀히는 점프 카운트를 음이항 꼬리확률로 모델링 가능.)

**기본 파라미터:** k=3(또는 2.5로 완화, §3.3 연동), m=20d, τ_J=5.

---

### 3.3 C — 변동성 군집 강도 (Volatility Clustering) ★차별화 코어

> **⚠️ 설계 변경(2026-07-07, 외부 피드백 반영):** 당초 Hawkes를 대표 추정으로 두었으나, **일봉 기준 |u|>3 점프는 연 십수 건**에 불과해 3-파라미터 MLE 적합이 극도로 불안정하다. "표본 몇 개로 적합했나?"라는 심사 질문에 수리적 차별화가 역으로 약점이 된다. → **주(主) 추정을 GARCH(1,1) 지속성으로 전환**하고, Hawkes는 **장중 데이터 확보 시에만** 보조·대표로 승격. 발표 서사는 "**변동성 군집성 포착(GARCH 지속성 기반, 데이터 허용 시 Hawkes로 강화)**"로 완화.

**아이디어:** 환율 변동성은 **군집**한다(큰 움직임 뒤에 큰 움직임). 이 지속성을 정량화해 "경보의 선행성"을 확보.

**주(主) 추정 — GARCH(1,1) 지속성 (일봉, 표본 충분·안정):**
```
σ²_g,t = ω + α_g · r²_{t-1} + β_g · σ²_g,t-1
persistence = α_g + β_g          # 1에 가까울수록 강한 군집(충격이 오래 감)
```
`persistence`는 해석이 명확하고(≈0.9+면 강한 군집), 일봉 표본으로 **안정적으로** 추정된다.

> **⚠️ 정상성 폭발 제약 & 폴백(2026-07-07 피드백 반영):** 2022 달러 강세 등 극단 국면 백테스트에서 무제약 MLE는 `α_g+β_g≥1`로 새어 무조건분산 `ω/(1−α_g−β_g)`이 무한대·음수가 되며 **연산 폭발(런타임 에러)**. → MLE 최적화에 **제약 `α_g,β_g>0, α_g+β_g ≤ 0.999`**를 삽입하고, 수렴 실패·제약 위반 시 **EWMA(λ=0.94)로 폴백**하는 루틴을 둔다.

> **⚠️ z-score 소거 문제 & 재정의(2026-07-07 피드백 반영):** 당초 `C_raw = (α_g+β_g)·(σ²_g,t/σ̄²)` 후 z-score였는데, **배치 내 상수** `c=(α_g+β_g)/σ̄²`는 z-score(또는 percentile)에서 `z(c·X)=z(X)`로 **완전 소거**된다 → persistence 곱셈이 무의미해지고 C가 "조건부분산 레벨"로 전락, **V(변동성 레벨)와 중복**. → C를 **레벨과 직교하는 '군집 동역학'**으로 재정의한다.

**재정의된 C — 분산 가속도(시변, 레벨-직교):**
```
C_raw,t = σ²_g,t / σ²_g,{t−m}          # m일 전 대비 조건부분산 변화율(군집이 '쌓이는 중'인가)
C_t     = PercentileRank_252( C_raw,t ) # 과거 252일 백분위(비선형 보존, 유계)
C_t     ← residualize(C_t | V_t)        # V에 대해 잔차화 → V의 증분 정보만 유지
```
- 레벨이 아니라 **변화율**이라 상수 소거·V 중복을 동시 회피. `corr(C,V)`를 리포트해 증분성 확인.
- **시변 persistence 대안:** 롤링창 재추정 `p_t`(상수 아님 → 소거 안 됨)를 `C_raw`로 써도 됨.
- **장중 확보 시 1순위:** Hawkes 분기비 `n_t`(아래 보조 추정)를 C로 승격.

**보조 추정 — Hawkes 자기여기 강도 (장중 데이터 확보 시에만):**
장중 점프 사건열 `{t_i}`에 대한 지수커널 Hawkes로 군집을 더 직접 포착:
```
λ(t) = μ + Σ_{t_i < t} α · e^{-β (t - t_i)}      # 분기비 n=α/β<1(정상성)
LL   = Σ_i ln λ(t_i) − [ μT + (α/β) Σ_i (1 − e^{-β(T - t_i)}) ]
```
장중 데이터는 사건 수가 충분해 MLE가 안정적 → 이때만 대표로 승격, `n`을 군집강도 스칼라로 사용.

**의사결정 규칙(데이터로 확정):**
1. Phase 1에서 **실제 점프 사건 수를 먼저 리포트**한다.
2. 일봉만 → **GARCH 지속성이 대표**, Hawkes는 언급만.
3. 장중 확보 & 안정 → **Hawkes 승격**, GARCH는 교차검증용.
4. 점프 임계 `k`를 **2.5로 낮춰** 사건 수를 늘리는 것도 병행 검토(§3.2와 연동).

---

### 3.4 M — 거시 이벤트 근접도 (Macro Event Proximity)

**아이디어:** FOMC·CPI·고용·한은 금통위 등 **예정된 리스크 이벤트가 임박**하면 위험 고조.

이벤트 `e_j`(예정일 `d_j`, 중요도 가중 `w_j∈{FOMC:1.0, CPI:0.8, 고용:0.7, 금통위:0.9, 무역지표:0.5,…}`)에 대해:
```
M_raw,t = max_{d_j ≥ t} [ w_j · e^{-(d_j - t)/τ_M} ]     # 가장 임박한 큰 이벤트가 지배
M_t     = min(1, M_raw,t)                                # 이미 0~1 스케일
```
(이벤트 **직후** 잔여 불확실성 반영이 필요하면 대칭 커널로 소폭 확장 가능.)

**기본 파라미터:** τ_M=3영업일. 캘린더 소스 = §7 데이터.

---

### 3.5 S — 뉴스·심리 텐션 (Sentiment Tension) [라이브 전용·백테스트 제외]

> **⚠️ 검증 정합성 규칙(2026-07-07, 피드백 반영):** 과거 시점의 뉴스 톤을 **룩어헤드 없이 재현**하는 것은 사실상 불가능하다(당시 시점 뉴스 아카이브 + 당시 기준 LLM 판단 필요). 이는 "모든 요소를 walk-forward로 검증한다"는 원칙과 정면 충돌한다. → **S는 백테스트/성능검증에서 완전히 제외**(FX-EWI 검증은 V·J·C·M만으로 수행)하고, **라이브 데모에서만 보조 표시**로 운용한다. 이로써 검증 서사의 일관성을 지킨다.

**아이디어:** 거시 뉴스 톤·지표 서프라이즈의 위험회피(risk-off) 신호. **노이즈가 크고 무누수 검증이 불가능하므로**, 라이브에서 다른 요인을 증폭하는 참고 역할로만.

```
tone_t ∈ [-1, 1]      # LLM 기반 최근 D일 거시뉴스 톤(음수=risk-off)
surp_t ∈ [-1, 1]      # 경제지표 서프라이즈 방향(실제-컨센서스, 확보 시)
S_raw  = 0.6·(-tone_t) + 0.4·(-surp_t)      # risk-off일수록 ↑
S_t    = Φ_L( z(S_raw) )
```
> **✅ S는 어떤 숫자 지수에도 들어가지 않는다 — 이중잣대 완전 제거(2026-07-08 재검토 반영, 외부조언④):** 당초 라이브에 한해 `score_live = score + w_S·S_eff`로 S를 **숫자에 가산**했으나, 이는 *검증한 지수 ≠ 배포하는 지수*라는 잔여 모순(PBO/DSR 신뢰성 훼손)을 남긴다. → **S를 라이브 가산항에서도 삭제.** S는 오직 **② Analyst의 정성 XAI/게이팅**으로만 쓴다("뉴스도 risk-off이니 주의" 같은 자연어 맥락·오탐 억제). 결과: **배포 FX-EWI 숫자 = 검증 FX-EWI 숫자**로 완전 일치. `S_eff` 게이팅 수식은 폐기(정성 판단으로 대체).

### 3.6 정책개입 국면 플래그 (KRW 특화) ★차별화 (2026-07-08 재검토 반영, 외부조언①)

> **왜 독립 팩터인가:** 원/달러는 1,300·1,400원 등 라운드넘버에서 **외환당국 스무딩(실개입)**이 실재하는 KRW 특유 현상이다. 개입은 실현변동성을 **인위적으로 누른다** → vol 기반 V·C·J가 개입 구간에서 **오히려 경보를 낮게** 낸다. 그러나 개입이 실패하면 그때가 최대 돌파위험. 즉 개입은 **"눌린 변동성 + 높은 돌파위험"**이라는, 감쇄가 아니라 정반대 신호다. → **M(예정 이벤트 캘린더)에 묻으면 안 된다**(M-ablation 오염, 개입은 레벨의존·비예정). 독립 플래그 + 방향층으로 처리한다.

```
Round_t   = exp( −min_j |P_t − L_j| / τ_R )        # L_j∈{1300,1400,…} 라운드넘버 근접도
Interv_t  = w_R·Round_t + w_news·개입뉴스텐션 + w_flow·임계부근_일방향흐름   # 0~1
IZ_t      = 1{ Interv_t > θ }                       # 정책개입 국면 여부
```
- **용도 1(지수 아님):** IZ 국면을 **표시**만 한다(지수를 낮추지 않음). "변동성은 눌렸으나 돌파위험 잠복" 배지.
- **용도 2(방향층 보정):** IZ 국면에서 §4.3 예산환율 이탈확률 분포를 **비대칭 절단(truncation)**으로 보정 — 개입은 원화강세 저지(방향성)이므로 방향층이 제자리. 개입 방향으로의 이탈확률은 낮추고, 개입 실패 시 반대꼬리는 팻테일 유지.
- **파라미터:** τ_R(원 단위 근접 스케일), θ(국면 임계) — walk-forward 규율 대상.

---

## 4. 합성 (Composite) 및 등급

> **✅ 2-헤드 구조(2026-07-08 재검토 반영, D§②):** FX-EWI는 같은 특징(V·J·C·M)을 공유하는 **두 출력 헤드**를 갖는다 — ⑴ **확률 헤드**(0~100 경보 등급, 아래 §4.1) + ⑵ **σ̂_{t+N} 회귀 헤드**(수익률 단위 변동성 예측, §4.3 방향층·경제성 백테스트에 σ_fwd 공급). 검증 손실은 헤드별로 다르다: 확률 헤드=ROC/PR-AUC·Brier, 회귀 헤드=**QLIKE**(변동성 예측 표준손실)·MSE. 회귀 헤드 덕에 GARCH와 직접 비교(§6.1)가 성립한다.

### 4.1 지도학습 캘리브레이션 (권장)

**라벨 정의(예측 대상):** 시점 t에서 향후 N일 위험 고조 여부.
```
Y_t = 1{ RV_[t+1, t+N] > Q_p( RV_forward ) }   # 미래 실현변동성이 롤링 p분위 초과
```
(대안 라벨: `[t+1,t+N]`에 점프 발생, 또는 최대 낙폭 임계 초과 — 3종 비교 후 채택)

**정규화 선형결합 → 확률 (검증 대상 = V·J·C·M):**
```
score_t  = w_V·V + w_J·J + w_C·C + w_M·M          # ← S 제외(§3.5): 백테스트/검증은 이 4요인만
FX-EWI_t = 100 · Φ_L( a·(score_t) + b )
# S 가산항 폐기(외부조언④·§3.5): 라이브 지수도 이 4요인만 → 배포 숫자 = 검증 숫자
```
- 가중치 `w_*` 와 보정 `a,b`는 **학습셋에서 로지스틱 회귀 + L2 정규화**로 추정(과최적화 억제).
- 사후 **isotonic/Platt 캘리브레이션**으로 "FX-EWI/100 ≈ 실제 위험확률"이 되도록 보정 → **FX-EWI는 곧 확률**이라 검증·설명이 깔끔.

**해석 대비 규칙기반 폴백:** 라벨 표본이 부족하면 사전가중 `w=(0.33,0.22,0.28,0.17)`(V,J,C,M)로 시작 후 점진 학습.

### 4.2 경보 등급 (Thresholds)

| 등급 | FX-EWI | 의미 |
|---|---|---|
| 정상 | 0–40 | 평상 |
| 주의 | 40–60 | 관찰 강화 |
| 경계 | 60–80 | 헤지 검토 트리거 |
| 심각 | 80–100 | 즉시 대응 권고 |

임계치(40/60/80)는 임의값이 아니라 **학습셋에서 목표 정밀도(precision)·리드타임을 만족하도록 캘리브레이션**하고, **검증셋에서만 성능을 보고**한다.

**등급 히스테리시스(디바운싱) — alarm fatigue 2차 방어(2026-07-07 피드백 반영):** 배치 모수 고정만으로는 임계 부근 떨림이 남는다. 등급 전이에 **관성**을 준다.
- **상향**: 상위 임계 초과가 **연속 2영업일** 지속돼야 등급 승격(단발 스파이크 무시).
- **하향**: 더 낮은 임계(예: 경계→주의는 55 하회)에서만 강등(밴드 이력현상).
→ 담당자에게 가는 "심각" 알림이 하루짜리 노이즈로 남발되지 않는다.

### 4.3 예산환율 이탈확률 — 방향성 손실 레이어 ★핵심 보강

> **⚠️ 설계 보강(2026-07-07, 피드백 반영):** FX-EWI는 **양방향 난류(turbulence)**를 예측한다. 그러나 수출기업의 손실은 **방향성**이다(원화 강세 = 수출대금 원화환산 손실; 원화 약세는 오히려 이익). FX-EWI만으로 헤지를 권하면 "달러 오르는 국면에도 수출기업에 선물환 매도를 시키냐"는 반문에 무너진다. → **아래 방향층을 추가**한다.

**핵심 원칙: 환율 방향을 "예측"하지 않는다.** 원/달러 방향은 근사적 랜덤워크라, 방향 예측 모델은 오히려 신뢰를 깎는다. 방향성은 **기업의 이미 알려진 포지션 + 예산환율(budget rate)**에서 결정론적으로 나온다.

**정의:**
- 기업 포지션 `d ∈ {수출: 달러 롱, 수입: 달러 숏}`
- 예산환율(손익분기) `K` : 사업계획·원가에 반영된 기준 환율
- 향후 N일 변동성 `σ_fwd` — **FX-EWI의 vol-forecast 헤드가 직접 출력**(아래 주석, §4.1).

> **✅ σ_fwd 빈 고리 해소 — vol-forecast 헤드 신설(2026-07-08 재검토 반영, D§②):** 당초 "FX-EWI(0~100 난류지수)에서 σ_fwd를 매핑"은 **변환 함수가 정의되지 않은 빈 고리**였다(0~100 → 수익률 단위 변동성). LossAlert·경제성 백테스트 전체가 이 값에 의존하므로, **FX-EWI에 캘리브레이션된 σ̂_{t+N}(수익률 단위) 회귀 헤드를 신설**해 σ_fwd를 직접 조달한다. 부수효과: **GARCH와 사과 대 사과(둘 다 vol forecast)로 직접 비교** 가능해져 §6.1 증분가치 주장이 강화되고, §6의 PBO/DSR-분류기 오용 문제(D§③)도 소멸한다.

**예산환율 이탈확률(불리한 방향으로만):**
```
수출기업:  P_breach = P( S_{t+N} < K )   ≈ Φ_N( (ln K − ln S_t) / (σ_fwd·√(N/252)) )
수입기업:  P_breach = P( S_{t+N} > K )   (부호 반전)
# drift는 랜덤워크 가정으로 0 근사(무예측). σ_fwd는 FX-EWI 난류에서 조달.
```
> **⚠️ 스케일 정합(2026-07-07 피드백 반영):** `σ_fwd`는 **연율화 변동성**이므로, N일 지평으로 환산할 때 분모는 `σ_fwd·√(N/252)`다. `σ_fwd·√N`으로 쓰면 √252≈15.87배 과대평가된다(예: σ연율 10%, N=10일 → 잘못: 0.316 / 올바름: **0.0199**). (대안: σ를 애초에 일변동성으로 정의하고 `√N` 사용 — 둘 중 하나로 차원 일치.)

> **⚠️ 팻테일 보정 — 정규분포 대신 Student-t/경험분포(2026-07-07 피드백 반영):** 표준정규 CDF `Φ_N`은 외환의 **두터운 꼬리·왜도**를 무시해 예산환율 돌파(꼬리) 위험을 **과소평가**한다. → `Φ_N`을 **Student-t CDF(자유도 ν로 팻테일 반영, 강건·파라미터 1개)** 또는 **과거 N일 수익률의 경험분포(부트스트랩)**로 교체한다.
> *Gram-Charlier 확장은 채택하지 않음* — 초과첨도가 조금만 커도 밀도가 음수·CDF 비단조가 되어 FX 팻테일에서 수치적으로 불안정하다. Student-t/경험분포가 더 강건하다.

> **⚠️ 정책개입 국면의 비대칭 절단(2026-07-08 재검토 반영, 외부조언①):** §3.6 IZ 국면(라운드넘버+개입)에서는 미래 환율 분포가 **당국이 방어하는 방향으로 절단(truncation)**된다(예: 1,400 상단에서 원화 추가 약세 저지). → 이탈확률 `P_breach` 계산 시 IZ 국면이면 **개입 방향 꼬리는 축소, 반대 꼬리(개입 실패 시)는 팻테일 유지**하는 비대칭 분포를 쓴다. 이로써 "개입으로 눌린 구간에서 방향층이 위험을 과소평가"하는 오류를 막고, 개입 실패 돌파를 놓치지 않는다.

**손실 경보(방향성) = 난류 × 불리한 이탈:**
```
LossAlert = FX-EWI_t / 100  ×  P_breach       # 0~1
```
→ 원화 약세 급변(수출기업에 유리) 국면에선 `P_breach`가 낮아 손실경보가 **자동으로 눌린다.** FX-EWI(심각)라도 유리한 방향이면 헤지를 밀지 않는다. 반대로 원화 강세 난류에선 손실경보가 치솟는다.

**액션 프레이밍(중요):** 손실경보는 헤지 **'실행' 명령이 아니라 헤지 '비율 재검토' 트리거**다. 예: `목표 헤지비율 = f(LossAlert)` 로 난류·불리방향일수록 커버리지 상향 검토를 **제안**(결정은 사용자·영업점). → §(C) 경제성 백테스트의 동적 헤지 정책이 이 값을 사용한다.

FX-EWI는 정규화 요인의 (로지스틱) 선형결합이므로, 각 요인의 기여를 그대로 노출:
```
contrib_i = w_i · x_i ,   share_i = contrib_i / Σ_k contrib_k   (%)
```
표시 예: *"오늘 경보(EWI 72·경계)는 클러스터링 C 41% + 거시근접 M 33% + 변동성국면 V 18%가 견인. 뉴스 S는 8%."* → 2025 입상작의 "판단 근거 투명 공개" 코드를 수식으로 충족.

---

## 6. 검증 지표 (AUTO_BUY_SELL 하네스 연동)

> **✅ 검증도구 층별 재배치(2026-07-08 재검토 반영, D§③):** **PBO/DSR은 전략 Sharpe의 다중시험 보정 도구**(`overfitting.py`도 Sharpe 기준)이므로 **분류/예측 성과지표(AUC·Brier)에 붙이면 도구 오용**이다("그 통계량 원래 그런 용도 아닌데요"에 막힘). → **PBO/DSR은 `C §2` 경제성 P&L의 Sharpe에만 적용**(거기선 정확히 맞음, 하네스 그대로)하고, **예측층의 다중시험 방어는 Diebold-Mariano + White Reality Check / Hansen SPA**로 대체한다. σ̂ 회귀 헤드(§4.1) 덕에 예측층 비교가 vol-forecast 손실(QLIKE) 기반이라 DM/SPA가 자연스럽게 성립.

| 범주 | 지표 | 하네스 |
|---|---|---|
| 변동성 예측(회귀헤드) | **QLIKE**, MSE (vs GARCH 직접비교) | 신규(σ̂ 헤드) |
| 예측 성능(확률헤드) | ROC-AUC, PR-AUC, Precision/Recall/F1(등급별) | 신규(라벨 기반) |
| 확률 품질 | Brier score, 신뢰도 캘리브레이션 곡선 | 신규 |
| 조기성 | 리드타임 분포(경보→실제 급변까지 일수) | 이벤트 스터디 |
| 사건연구 | 경보 시 vs 평상시 **N일 후 실현변동성** 분포 비교 | 이벤트 스터디 |
| **예측층 다중시험** | **DM(HAC) + White RC / Hansen SPA** | 신규(예측층) |
| 무누수 | **Walk-forward** OOS 성능(학습=가중/임계 추정, 검증=측정) | `walk_forward.py` |
| (경제성층으로 이관) | **PBO/DSR** → `C §2.4` P&L Sharpe에 적용 | `overfitting.py` |

> **✅ 겹침 라벨·희소 에피소드 보정(2026-07-08 재검토 반영, D§④):** `[t+1, t+N]` 라벨은 N일씩 **겹쳐** 강한 자기상관 → 유효표본이 명목의 ~1/N. → ⑴ DM은 **Newey-West(HAC) 표준오차**, AUC/PR-AUC 신뢰구간은 **block bootstrap**(블록길이 ≥ N). ⑵ **독립 급변 국면 수를 먼저 세어 명시**하고 그에 맞는 효과크기만 주장(2022 달러 강세는 사실상 에피소드 1건). ⑶ Hawkes에 적용한 "표본 몇 개?"(§3.3) 잣대를 라벨 겹침에도 동일 적용.

### 6.1 베이스라인 대비 증분가치 검증 ★최우선 (2026-07-07 피드백 반영)

> **가장 큰 구멍:** 변동성은 지속성이 강해 예측이 "쉬운" 대상이다. 위 지표는 전부 **FX-EWI 자기 검증**일 뿐, **대안 대비 증분가치**가 아니다. "GARCH 하나보다 얼마나 더 맞히냐?"에 답하지 못하면 자체 지표가 '기존 모델 재포장'으로 격하된다. → 아래를 **필수**로 한다.

**베이스라인 3종(OOS 동일 조건 비교):**
| 베이스라인 | 정의 | 의미 |
|---|---|---|
| B0. 나이브 RV | "어제 변동성 = 내일 변동성"(persistence 1) | 최소 기준선 |
| B1. GARCH(1,1) | 표준 조건부 변동성 모델 | **진짜 경쟁자** |
| B2. 내재변동성 | 옵션 IV(확보 시) | 시장 컨센서스 |

**핵심 산출물 — 증분가치 표:** FX-EWI vs B0/B1/B2의 OOS ROC-AUC·PR-AUC·Brier를 나란히. **FX-EWI가 B1(GARCH)을 유의하게 이겨야** 한다.

**통계 검정:** 예측오차 차이에 **Diebold-Mariano 검정** → "FX-EWI의 오차가 GARCH보다 통계적으로 유의하게 낮다"를 p-value로 제시(반문 원천봉쇄).

**Ablation(요인 제거 실험) — 특히 M:**
- 각 요인(V·J·C·M)을 하나씩 빼고 성능 변화 측정.
- **M(캘린더) 제거 시 성능이 크게 안 떨어져야** 한다. 만약 M이 성능을 견인하면 → "그냥 경제 캘린더 알림 아니냐"는 공격 성립. 이 경우 **M을 보조 표시로 강등**하고 C·J(군집·점프)가 증분가치를 내도록 재설계.

**목표 문장(예):** "FX-EWI는 GARCH(1,1) 대비 OOS PR-AUC를 유의하게 개선(DM p<0.05), M 제거 후에도 우위 유지 → 증분가치는 캘린더가 아닌 변동성 군집·점프 구조에서 발생."

> **🔴 결과 — 이 목표는 기각됐다(2026-07-08 실측).**
> walk-forward OOS 에서 FX-EWI **AUC 0.577** < 단순 변동성 레벨 **0.596**. 지도학습 구제(`calibrate.py`)도 라벨 3종(level/jump/exp) 전부 레벨·GARCH에 패배(0.560 vs 0.593 / 0.464 / 0.515 vs 0.738). `VJC+LVL > VJC` → **신호는 레벨에 있고 V·J·C는 노이즈**. **증분가치 없음**이 결론이다.
> 위 검증 설계(베이스라인 3종·DM·Ablation)는 **계획대로 수행됐고, 그 결과가 우리 주장을 걷어냈다.** 하네스가 작동했다는 사실이 남은 자산이며, 자체 지표 가치는 **BBP**로 이동했다(`F` §3.1). FX-EWI 는 **XAI 위험게이지**로 강등해 배포한다 — 배포 숫자 = 검증 숫자.

### 6.2 스트레스 구간·자기검증
**대표 스트레스 구간:** 2022 달러 강세(원/달러 1,440원대), 2024~2025 변동 구간, 개별 급변일.
**목표 문장(예):** "경보(경계+) 발동 후 10일 실현변동성이 평상 대비 유의하게 상승(사건연구), PR-AUC OOS 견고, **PBO 낮음·DSR 유의**."

> **주의:** 위 자기검증은 **필요조건**일 뿐이다. 대상을 가르는 건 §6.1 증분가치와 §(C) 경제적 유의성이다.

---

## 7. 다통화·거래국 확장

- ~~기본은 **USD/KRW 집중**. 통화쌍별로 동일 파이프라인을 복제해 `FX-EWI^(c)` 산출 가능(모듈형).~~
  → **실행됨(2026-07-17): `USD·EUR·JPY` 개방** (`fx_sentinel/build_currencies.py` → `state/currencies.json`).
  - `components.py`·`fx_ewi.py` 가 DataFrame 을 받는 구조라 **통화 중립**이었다 — USD 에 묶인 건 `data_loader.py` 뿐이라 복제가 아니라 소스만 늘리면 됐다.
  - σ̂ = `ewma_vol(r)×√252` (σ̂ 헤드와 동일 식). **USD self-check 게이트**: 이 식으로 USD 를 재현해 기준일 σ̂ 0.098029 = 화면 상수 0.098, 캘리브레이션 ECE 0.0344·Brier 0.1754·3,800건이 기존 산출과 일치할 때만 나머지 통화를 쓴다. 어긋나면 빌드 중단.
  - **국면도 통화별 실측**: 같은 날짜에서 각 통화의 σ̂·EWI 를 직접 잰다 → USD 배수를 곱하던 구조가 사라졌다(달러 강세/현재 비율 USD 1.67× · EUR 0.99× · JPY 1.37× 로 갈림 = 배수가 아님의 증거).
  - **캘리브레이션은 통화별로 따로**: USD 0.034 / EUR 0.081 / JPY 0.072. 신규 통화가 나쁘다 — `drift=0` 가정이 추세성 크로스에서 꼬리를 덜 맞힌다. 숨기지 않고 통화 선택 자리에 노출한다.
  - **CNY 미개방**: FDR `KRW/CNY` 의 호가 관례가 구간마다 100배 바뀐다(2018년 통째·2020년 1~2월 → 일수익률이 정확히 `ln(100)`). **구간 전체가 같은 오류라 국소 중앙값 이상치 필터를 조용히 통과**해 COVID 국면 σ=8.76(연율 876%)이 나왔다 → 필터를 드롭이 아닌 **예외(위생 게이트)**로 전환. KB 고시환율 연동 시 즉시 개방 가능. VND 는 관리변동환율이라 실현변동성이 시장 리스크를 대표하지 못한다.
- **거래국 리스크**는 FX-EWI(통화 변동성)와 **분리된 국가 매크로 점수**(성장률·물가·금리·경상수지, §Advisor 데이터)로 별도 산출 후 대시보드에서 결합 표시. → #6 "거래국 경제상황 리스크" 요건 충족.

---

## 8. 구현 노트

- **데이터 주기:** 일 1회 종가 기준 산출(장중 확장 가능). 모든 입력은 t 종가까지.
- **재적합 주기 & 모수 표류 방지(2026-07-07 피드백 반영):** GARCH 등 **모수는 매일 재학습하지 않는다.** 매일 재적합하면 지수(FX-EWI)가 erratic하게 튀어 **알림 피로(alarm fatigue)·시스템 불신**을 유발한다. →
  - **모수 추정 = 월 1회 야간 배치**(GARCH `ω,α_g,β_g` / 로지스틱 가중 `w` / 임계치)로 **고정**. walk-forward 창과 정렬.
  - **일간 실시간 = 고정 모수로 지수만 산출**(관측치만 갱신). EWMA 분산은 재귀 갱신(모수 λ 고정).
  - 배치 갱신 시 이전/신규 모수 성능을 검증셋서 비교 후 스왑(회귀 방지).
- **regime break 대응 — 이벤트드리븐 오프사이클 재적합(2026-07-08 재검토 반영, 외부조언②):** "월 1회 고정"의 맹점 = 블랙스완 시 구조가 하루 만에 바뀌는데 GARCH 지속성 C가 늦게 따라옴. **단, 빠른 성분 V(EWMA)·J(점프)는 고정모수로 매일 재귀갱신되어 블랙스완을 당일 포착**하므로 "수 주 오작동"은 아니다(느린 건 C 하나). → 평시 월고정을 유지하되, **V·J가 극단 임계를 돌파하면 그날 즉시 GARCH를 오프사이클 재적합**한다(상시 일일적합의 erratic 노이즈 없이 regime break만 포착). **고도화 경로:** 전일 모수로 릿지하는 **L2-정규화 일일적합** 또는 **칼만 필터 스무딩**(erratic 일일 vs 낡은 월고정의 정답 중간). 단, 새 하이퍼파라미터(L2 λ / 칼만 Q,R)도 walk-forward 규율 대상.
- **정상성 체크:** Hawkes `n=α/β<1` 강제, 위반 시 폴백.
- **결측·휴장:** 영업일 캘린더 정렬, 결측 전일 보간 금지(누수 위험) → 직전 관측 유지.
- **산출물 스키마(예):** `{date, fx_ewi, grade, comp:{V,J,C,M,S}, shares:{...}, hawkes:{mu,alpha,beta,n}}` → 대시보드/알림/검증 공용.

---

## 9. 착수 전 확정할 파라미터 (열린 결정)

1. **예측 지평 N**: 5 / 10 / 20 중 대표값(권장 10, 3종 비교 후 확정)
2. **라벨 정의**: 실현변동성 분위 초과 / 점프 발생 / 최대낙폭 중 채택(권장 실현변동성)
3. **점프 임계 k**: 3 고정 vs 학습셋 분위 적응
4. **Hawkes 커널**: 단순 지수 vs marked(크기 반영)
5. **합성 방식**: 지도학습 캘리브레이션(권장) vs 사전가중 규칙기반(MVP)

> 위 5개는 Phase 1 착수 시 데이터로 결정. 나머지 수식·파이프라인은 본 문서로 확정.

> **✅ 메타 파라미터 사전등록(2026-07-08 재검토 반영, D§⑤):** N·라벨·k는 **하네스 바깥의 연구자 자유도**라 PBO가 못 잡는다(PBO는 하네스 *안*의 임계치·가중 조합만 감시). 여기서 OOS 보고 고르면 과최적화가 PBO를 우회한다. → **주 스펙(N=10, RV분위 라벨, k=3)을 OOS 열어보기 전에 사전등록(pre-register)**하고, 나머지 조합은 secondary로만 보고한다. 이 한 줄이 "튜닝 운 아니냐"를 원천봉쇄.
